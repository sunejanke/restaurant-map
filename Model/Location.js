// a prototype that stores information for later use 
function Location(title, lat, lng, type, url){
    this.Title = title;
    this.Latitude = lat;
    this.Longitude = lng;
    this.Type = type;
    this.Url = url;
}